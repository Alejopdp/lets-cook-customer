import { Typography } from '@material-ui/core';
import Layout from '../../layout';

const HomePage = () => {
    return <Layout>
        <Typography variant="h2" color="error">Home Page</Typography>
    </Layout>
}

export default HomePage;